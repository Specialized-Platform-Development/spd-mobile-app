
const CONFIG = {
    // Use 10.0.2.2 for Android Emulator, localhost for iOS Simulator
    // Replace with local IP (e.g., 192.168.1.x) for physical device
    API_URL: 'https://spd-backend-api.onrender.com/api',
};

export default CONFIG;
